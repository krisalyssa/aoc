#!/bin/bash

##
## run code for a year and day
##

usage() {
        echo "usage: run DAY" 2>&1
        exit 1
}

DAY=$1
[[ -z ${DAY} ]] && usage

shift 1

cd /aoc/src || exit 1

gleam run -m "day_${DAY}"
