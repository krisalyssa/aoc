#!/bin/bash

##
## initialization which needs to take place inside the container
##

usage() {
        echo "usage: init" 2>&1
        exit 1
}

cd /aoc/src || exit 1

# update zig
# install any packages (does zig have packages?)
